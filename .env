# Data source toggle:
#   VITE_USE_MOCK=true   -> use built-in mock data (works with no backend)
#   VITE_USE_MOCK=false  -> call the real MRI backend at VITE_API_BASE_URL
VITE_USE_MOCK=true

# Backend base URL (used only when VITE_USE_MOCK=false).
VITE_API_BASE_URL=http://localhost:8000
